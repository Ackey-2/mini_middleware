syntax = "proto3";

package mm;

message RpcRequest {
    uint64 request_id = 1;
    string reply_topic = 2;
    bytes payload = 3;
}

message RpcReply {
    uint64 request_id = 1;
    bytes payload = 2;
    bool ok = 3;
    string error = 4;
}
